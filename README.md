# Image-to-PDF-converter
Image-to-PDF Converter is a simple Python GUI application that allows users to select multiple images (PNG, JPG, JPEG) and convert them into a single PDF file. The application provides a friendly interface with Tkinter, displays the selected images, and lets users name the output PDF.
